// Placeholder package to avoid legacy require errors when RageMP loads "client_packages" as a server resource.
// All actual client logic lives under `server-files/client_packages` and is handled via CEF/JS files.

console.log('[client_packages] Placeholder server package loaded (no logic executed).');

module.exports = {};
